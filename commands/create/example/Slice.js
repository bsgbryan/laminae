import { createSlice } from '@reduxjs/toolkit'

import { GET } from 'PATHhelpers/api'
import { set } from 'PATHhelpers/redux'

const slice = createSlice({
  name: 'REPLACE',
  initialState: {
  },
  reducers: {
  }
})

export const {
} = slice.actions

const {
} = slice.actions

export default slice.reducer